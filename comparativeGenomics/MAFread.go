// Maf reader

package comparativeGenomics

import (
	"fmt"
)

// select the phylogeny to analyze

// as data is read in per block we can report back species specific gaps

// we could start by reading in the gaps

func main() {

	// only need one file
	// just need to correctly parse the file

}
